<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    
    <!-- VR Permissions for Quest 3S -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="horizonos.permission.HEADSET_CAMERA" />
    <uses-permission android:name="com.meta.permission.CAMERA" />
    <uses-permission android:name="com.oculus.permission.CAMERA" />
    <uses-permission android:name="com.oculus.permission.USE_SCENE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Quest 3S specific permissions -->
    <uses-permission android:name="com.oculus.permission.PASSTHROUGH" />
    <uses-permission android:name="com.meta.permission.PASSTHROUGH" />
    <uses-permission android:name="com.oculus.permission.USE_SCENE" />
    
    <!-- Camera Features -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="true" />
    <!-- VR Features (optional for testing) -->
    <uses-feature android:name="android.hardware.vr.headtracking" android:required="false" />
    <uses-feature android:name="oculus.software.handtracking" android:required="false" />
    <uses-feature android:name="android.hardware.vr.high_performance" android:required="false" />
    <!-- Quest 3S Passthrough Camera Support -->
    <uses-feature android:name="com.meta.feature.PASSTHROUGH" android:required="false" />
    
    <application
        android:label="AI-Companion VR"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- VR Meta Data for Quest 3S -->
        <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />
        <meta-data android:name="com.oculus.supportedDevices" android:value="quest3s|quest3|quest2" />
        <meta-data android:name="com.oculus.handtracking.frequency" android:value="HIGH" />
        <meta-data android:name="com.oculus.handtracking.version" android:value="V2.0" />
        <!-- Quest 3S Passthrough Camera -->
        <meta-data android:name="com.meta.passthrough.enable" android:value="true" />
        <meta-data android:name="com.oculus.passthrough.enable" android:value="true" />
        <meta-data android:name="com.meta.camera.headset" android:value="true" />
        <meta-data android:name="com.oculus.camera.headset" android:value="true" />
        <meta-data android:name="horizonos.camera.enable" android:value="true" />
        <!-- Passthrough Mixed Reality -->
        <meta-data android:name="com.oculus.intent.category.VR" android:value="true" />
        <meta-data android:name="com.oculus.supportedDevices" android:value="quest3s|quest3|quest2" />
        <meta-data android:name="com.meta.mixed_reality" android:value="true" />
        <!-- Permission Requirements -->
        <meta-data android:name="com.meta.permission.CAMERA.required" android:value="true" />
        <meta-data android:name="horizonos.permission.HEADSET_CAMERA.required" android:value="true" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
